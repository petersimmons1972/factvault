apiVersion: batch/v1
kind: CronJob
metadata:
  name: factvault-dossier
spec:
  schedule: "*/15 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      activeDeadlineSeconds: 600
      backoffLimit: 1
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            fsGroup: 65532
          initContainers:
            - name: factvault-migrate
              image: factvault-app:latest
              args: ["factvault", "migrate"]
              envFrom:
                - configMapRef:
                    name: factvault-config
                - secretRef:
                    name: factvault-db-credentials
                # Migrations create extensions and run DDL (CREATE EXTENSION
                # pgcrypto/vector/pg_trgm, GRANTs), which the runtime app_user DSN
                # above cannot do. FACTVAULT_MIGRATE_DATABASE_URL from this Secret
                # takes precedence in cmd/factvault/migrate.go; only this
                # initContainer receives it. See #272.
                - secretRef:
                    name: factvault-migrate-credentials
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                runAsNonRoot: true
                runAsUser: 65532
                readOnlyRootFilesystem: true
                seccompProfile:
                  type: RuntimeDefault
              resources:
                requests:
                  cpu: "50m"
                  memory: "64Mi"
                limits:
                  cpu: "200m"
                  memory: "128Mi"
          containers:
            - name: factvault-dossier
              image: factvault-app:latest
              args: ["factvault", "worker", "dossier", "--tenant", "$(FACTVAULT_TENANT_ID)"]
              envFrom:
                - configMapRef:
                    name: factvault-config
                - secretRef:
                    name: factvault-db-credentials
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
                runAsNonRoot: true
                runAsUser: 65532
                readOnlyRootFilesystem: true
                seccompProfile:
                  type: RuntimeDefault
              resources:
                requests:
                  cpu: "100m"
                  memory: "256Mi"
                limits:
                  cpu: "500m"
                  memory: "1Gi"
