apiVersion: batch/v1
kind: CronJob
metadata:
  name: factvault-extract
spec:
  schedule: "*/15 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            fsGroup: 65532
          containers:
            - name: factvault-extract
              image: factvault-app:latest
              args: ["/bin/sh", "-c", "factvault migrate && factvault worker extract --tenant $(FACTVAULT_DEV_TENANT_ID)"]
              envFrom:
                - configMapRef:
                    name: factvault-config
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
