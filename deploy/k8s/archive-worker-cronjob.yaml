apiVersion: batch/v1
kind: CronJob
metadata:
  name: factvault-archive
spec:
  schedule: "*/15 * * * *"
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: OnFailure
          securityContext:
            fsGroup: 65532
          containers:
            - name: factvault-archive
              image: factvault-app:latest
              args: ["factvault", "worker", "archive"]
              envFrom:
                - configMapRef:
                    name: factvault-config
              securityContext:
                allowPrivilegeEscalation: false
                capabilities:
                  drop: ["ALL"]
