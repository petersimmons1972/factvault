services:
  searxng:
    image: searxng/searxng:latest
    ports:
      - "8088:8080"

  factvault-api:
    environment:
      FACTVAULT_WAYBACK_URL: "https://web.archive.org"

  factvault-workers:
    environment:
      FACTVAULT_WORKER_INTERVAL_SECONDS: "60"
